import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'sl-dev-screens-connector',
  webDir: 'www'
};

export default config;
